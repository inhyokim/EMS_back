-- EMS Database Initialization Script
-- 실제 스키마는 JPA DDL(auto) 또는 Flyway로 관리 권장

-- 예시: 초기 관리자 설정이나 기본 데이터
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- 예시: 기본 시드 데이터 (필요시 추가)
-- INSERT INTO device (name, type, location, active, created_at) 
-- VALUES ('Default Meter', 'METER', 'Main Building', true, NOW())
-- ON CONFLICT DO NOTHING;

